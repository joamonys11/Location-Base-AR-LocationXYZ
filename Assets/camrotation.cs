using UnityEngine;
using System.Collections;

public class camrotation : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	


//		Quaternion currentGyro = Input.gyro.attitude;
//		transform.localRotation = currentGyro * Quaternion.Euler (0, 0, 90);

//		print ("X " + Input.gyro.rotationRateUnbiased.x);
//		print ("Y " + Input.gyro.rotationRateUnbiased.y);
//		print ("Z " + Input.gyro.rotationRateUnbiased.z);
	}
}
